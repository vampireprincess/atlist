import { ComingSoonPanel } from './ComingSoonPanel';

export function AnimationsPanel() {
  return (
    <ComingSoonPanel
      title="Animations"
      phase="Phase 2"
      description="A full library of marker animations (pulse, bounce, glow, ripple, radar pulse, heartbeat, breathing, path reveal and more) with triggers, easing, random variation and prefers-reduced-motion support."
    />
  );
}
