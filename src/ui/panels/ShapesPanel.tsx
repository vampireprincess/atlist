import { ComingSoonPanel } from './ComingSoonPanel';

export function ShapesPanel() {
  return (
    <ComingSoonPanel
      title="Shapes & Regions"
      phase="Phase 3"
      description="Polygons, circles, rectangles, custom shapes and GeoJSON regions with hover, click, popups, labels and choropleth support."
    />
  );
}
