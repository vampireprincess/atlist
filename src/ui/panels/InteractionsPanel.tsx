import { ComingSoonPanel } from './ComingSoonPanel';

export function InteractionsPanel() {
  return (
    <ComingSoonPanel
      title="Interactions"
      phase="Phase 2"
      description="Visual Interaction Builder with click / hover / viewport / filter triggers and chained actions (open popup, zoom, animate, open URL, directions, sidebar tabs, custom events…)."
    />
  );
}
